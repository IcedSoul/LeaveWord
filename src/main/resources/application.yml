server:
  port: 8081
  servlet:
    context-path: /LeaveWord

# 现在推荐新的方式Spring Boot Maven Plugin
spring:
  profiles:
    active: @spring.active@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf:
    prefix: classpath:/static/
    suffix: .html
    cache: false


